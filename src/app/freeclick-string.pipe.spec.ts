import { FreeclickStringPipe } from './freeclick-string.pipe';

describe('FreeclickStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FreeclickStringPipe();
    expect(pipe).toBeTruthy();
  });
});
